# 数据库升级说明

## ⚠️ 重要提示

在集成Whisper功能时，数据库从v1升级到了v2，添加了`transcriptionText`字段。

### 数据库变更
- **版本**: v1 → v2
- **新增字段**: `transcriptionText` (转录文本)
- **迁移策略**: `fallbackToDestructiveMigration()` (开发阶段)

### 影响
使用`fallbackToDestructiveMigration()`会导致：
- ✅ 应用可以正常运行
- ❌ **旧数据会被清空**
- ❌ 之前的录音记录会丢失

### 解决方案

#### 方案1：重新录音（推荐用于测试）
1. 点击"开始监听"
2. 对着手机说话
3. 等待录音完成
4. 查看录音列表按钮会出现

#### 方案2：添加数据库迁移（生产环境）
在`DatabaseModule.kt`中添加迁移：

```kotlin
val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(database: SupportSQLiteDatabase) {
        // 添加新字段
        database.execSQL("ALTER TABLE recordings ADD COLUMN transcriptionText TEXT")
    }
}

@Provides
@Singleton
fun provideAppDatabase(@ApplicationContext context: Context): AppDatabase {
    return Room.databaseBuilder(
        context,
        AppDatabase::class.java,
        "voice_assistant_db"
    )
    .addMigrations(MIGRATION_1_2)  // 使用迁移而不是删除数据
    .build()
}
```

## 当前状态

### 录音列表按钮
- **显示条件**: `totalCount > 0`
- **位置**: 统计卡片底部
- **文本**: "📝 查看录音列表"

### 为什么看不到按钮？
从日志看：`录音统计: 1条`

可能原因：
1. 数据库升级清空了旧数据
2. 新录音还没有生成
3. UI还没有刷新

### 测试步骤
1. 确保服务正在运行（日志显示"服务状态: 运行中"）
2. 对着手机说话
3. 等待录音完成（日志会显示"✅ 已保存"）
4. 等待10秒自动刷新，或手动点击"🔄 刷新"
5. 按钮应该会出现

## Whisper模拟实现说明

### 当前状态
是的，Whisper目前使用的是**模拟实现**。

### 为什么是模拟？
真正的whisper.cpp需要：
1. 编译C++代码为Android JNI库
2. 生成.so文件（libwhisper.so）
3. 集成Java接口

这个过程比较复杂，所以我先创建了模拟实现让代码能编译运行。

### 模拟实现的行为
在`WhisperContext.kt`中：
```kotlin
fun transcribeData(audioData: FloatArray): String {
    // 返回模拟文本用于测试
    return "这是模拟的转录文本。实际使用时需要集成whisper.cpp库。音频长度：${audioData.size}样本"
}
```

### 如何启用真正的转录？

#### 步骤1：编译whisper.cpp
```bash
git clone https://github.com/ggerganov/whisper.cpp.git
cd whisper.cpp
mkdir build-android && cd build-android
cmake -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK/build/cmake/android.toolchain.cmake \
      -DANDROID_ABI=arm64-v8a \
      -DANDROID_PLATFORM=android-26 \
      ..
make
```

#### 步骤2：复制.so文件
```
app/src/main/jniLibs/
  arm64-v8a/
    libwhisper.so
  armeabi-v7a/
    libwhisper.so
```

#### 步骤3：替换模拟实现
删除`app/src/main/java/com/whispercpp/whisper/WhisperContext.kt`，使用whisper.cpp提供的Java接口。

#### 步骤4：测试
1. 录制音频
2. 查看转录结果
3. 验证准确度

## 快速测试指南

### 测试录音功能
1. 打开应用
2. 点击"开始监听"
3. 对着手机说话（说几句话）
4. 等待10-15秒
5. 查看日志：应该看到"✅ 已保存: voice_xxx.wav"
6. 点击"🔄 刷新"按钮
7. "📝 查看录音列表"按钮应该出现

### 测试转录功能（模拟）
1. 录制一段音频
2. 等待5秒（转录服务处理间隔）
3. 查看日志：
   - "🎯 开始转录: voice_xxx.wav"
   - "✅ 转录完成: 这是模拟的转录文本..."
4. 点击"📝 查看录音列表"
5. 应该能看到转录文本（模拟的）

## 总结

- ✅ 录音功能正常工作
- ✅ 转录服务正常运行
- ✅ Whisper引擎已初始化
- ⚠️ 使用模拟转录（需要集成真正的whisper.cpp）
- ⚠️ 数据库升级可能清空了旧数据
- 💡 重新录音即可看到录音列表按钮
