# VoiceLife 存储架构说明

## 双重存储机制

录音数据采用**双重存储**机制，确保数据安全和便于管理：

### 1. 物理文件存储
**位置**: `/sdcard/Download/VoiceAssistant/`

**目录结构**:
```
/sdcard/Download/VoiceAssistant/
├── pending/        # 待转换的录音（原始WAV文件）
├── processing/     # 转换中的录音
├── completed/      # 已转换的录音（保留7天）
└── failed/         # 转换失败的录音
```

**文件命名**: `voice_yyyyMMdd_HHmmss.wav`
- 例如: `voice_20251206_110707.wav`
- 格式: 16kHz, 单声道, 16bit PCM WAV

**优点**:
- ✅ 用户可以直接访问
- ✅ 可以用任何音频播放器播放
- ✅ 可以手动复制、分享
- ✅ 不依赖应用即可访问

### 2. 数据库存储
**位置**: 应用私有数据库 (Room Database)

**存储内容**:
```kotlin
data class Recording(
    @PrimaryKey(autoGenerate = true)
    val id: Long = 0,
    val filePath: String,           // 文件路径
    val fileName: String,            // 文件名
    val duration: Int,               // 时长（秒）
    val fileSize: Long,              // 文件大小（字节）
    val status: RecordingStatus,     // 状态
    val createdAt: Long,             // 创建时间
    val transcribedAt: Long? = null, // 转换时间
    val transcription: String? = null // 转换文本
)
```

**优点**:
- ✅ 快速查询和统计
- ✅ 支持状态管理
- ✅ 支持搜索和过滤
- ✅ 便于同步到云端

## 完整流程

### 录音保存流程

```
1. VAD检测到人声
   ↓
2. RecordingSession创建WAV文件
   位置: /sdcard/Download/VoiceAssistant/pending/voice_xxx.wav
   ↓
3. 持续写入音频数据
   ↓
4. 人声结束 + 10秒静音
   ↓
5. 停止录音，关闭文件
   ↓
6. 回调 onRecordingComplete(file)
   ↓
7. RecordingRepository.saveRecording(file)
   - 读取文件信息（大小、时长）
   - 创建数据库记录
   - 返回录音ID
   ↓
8. 完成！
   - 物理文件: /sdcard/Download/VoiceAssistant/pending/voice_xxx.wav
   - 数据库记录: recordings表中的一条记录
```

### 查看录音的方式

#### 方式1: 应用内查看
```
1. 打开VoiceLife应用
2. 查看"录音统计"卡片
3. 点击"打开文件夹"按钮
4. 查看日志中的文件列表
```

#### 方式2: 文件管理器
```
1. 打开手机的文件管理器
2. 进入 Download 文件夹
3. 找到 VoiceAssistant 文件夹
4. 进入 pending 子文件夹
5. 可以播放、分享、删除文件
```

#### 方式3: ADB命令
```bash
# 列出所有录音文件
adb shell ls -lh /sdcard/Download/VoiceAssistant/pending/

# 拉取单个文件到电脑
adb pull /sdcard/Download/VoiceAssistant/pending/voice_20251206_110707.wav .

# 拉取所有文件到电脑
adb pull /sdcard/Download/VoiceAssistant/pending/ ./recordings/
```

#### 方式4: 数据库查询
```bash
# 进入应用数据库
adb shell
su  # 需要root权限
cd /data/data/com.voicelife.assistant/databases/
sqlite3 voice_assistant.db

# 查询所有录音
SELECT * FROM recordings;

# 查询统计信息
SELECT status, COUNT(*), SUM(fileSize) FROM recordings GROUP BY status;
```

## 当前录音文件

根据最新检查，当前有**8个录音文件**：

| 文件名 | 大小 | 创建时间 |
|--------|------|----------|
| voice_20251206_095356.wav | 1.1MB | 09:54 |
| voice_20251206_102331.wav | 963KB | 10:24 |
| voice_20251206_105150.wav | 622KB | 10:52 |
| voice_20251206_105211.wav | 648KB | 10:52 |
| voice_20251206_110153.wav | 745KB | 11:02 |
| voice_20251206_110240.wav | 813KB | 11:03 |
| voice_20251206_110514.wav | 758KB | 11:05 |
| voice_20251206_110707.wav | 1.7MB | 11:08 |

**总计**: 约7.3MB，8段录音

## 数据库表结构

### recordings 表
```sql
CREATE TABLE recordings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    filePath TEXT NOT NULL,
    fileName TEXT NOT NULL,
    duration INTEGER NOT NULL,
    fileSize INTEGER NOT NULL,
    status TEXT NOT NULL,
    createdAt INTEGER NOT NULL,
    transcribedAt INTEGER,
    transcription TEXT
);
```

### 状态枚举
```kotlin
enum class RecordingStatus {
    PENDING,      // 待转换
    PROCESSING,   // 转换中
    COMPLETED,    // 已完成
    FAILED        // 失败
}
```

## 存储空间管理

### 自动清理策略

1. **过期录音清理**
   - 已完成的录音保留7天
   - 超过7天自动删除
   - 同时删除文件和数据库记录

2. **失败文件清理**
   - 转换失败的文件定期清理
   - 避免占用存储空间

3. **孤立文件清理**
   - 文件存在但数据库中没有记录
   - 定期扫描并清理

4. **空间不足处理**
   - 可用空间 < 500MB 时触发清理
   - 优先清理最旧的已完成录音

### 手动清理

在应用中点击"清理过期"按钮：
```
1. 删除7天前的已完成录音
2. 删除所有失败文件
3. 清理孤立文件
4. 显示清理结果
```

## 数据同步（Phase 5 - 待实现）

### 云端同步流程
```
1. 录音完成后加入上传队列
2. 后台上传到云端存储
3. 上传成功后更新数据库状态
4. 本地文件可选择保留或删除
5. 支持多设备同步
```

### 同步策略
- WiFi环境自动上传
- 移动网络可选择上传
- 支持断点续传
- 上传失败自动重试

## 数据安全

### 本地安全
- 文件存储在公共Download目录（用户可访问）
- 数据库存储在应用私有目录（需要root才能访问）
- 支持加密存储（可选）

### 云端安全
- 传输加密（HTTPS）
- 存储加密（AES-256）
- 访问控制（用户认证）
- 数据备份（多副本）

## 常见问题

### Q1: 为什么文件在Download文件夹？
A: 为了方便用户访问和管理。用户可以：
- 直接播放录音
- 分享给其他应用
- 手动备份到电脑
- 使用第三方工具处理

### Q2: 数据库存储了什么？
A: 数据库只存储元数据，不存储音频数据：
- 文件路径和名称
- 文件大小和时长
- 创建时间和状态
- 转换文本（如果有）

### Q3: 如何备份录音？
A: 三种方式：
1. 使用文件管理器复制整个VoiceAssistant文件夹
2. 使用ADB命令拉取到电脑
3. 等待云端同步功能（Phase 5）

### Q4: 删除应用后录音还在吗？
A: 
- ✅ 物理文件还在（Download文件夹）
- ❌ 数据库记录会丢失（应用私有数据）
- 建议：卸载前先备份或等待云端同步

### Q5: 如何查看数据库中的记录？
A: 在应用中查看"录音统计"：
- 总录音数
- 待处理数量
- 总时长
- 占用空间

### Q6: 文件会自动删除吗？
A: 
- pending文件夹：不会自动删除
- completed文件夹：7天后自动删除
- failed文件夹：定期清理
- 可以手动点击"清理过期"按钮

## 开发者信息

### 相关代码文件

1. **RecordingSession.kt** - 创建和写入WAV文件
2. **WavFileWriter.kt** - WAV文件格式处理
3. **RecordingRepository.kt** - 数据库操作
4. **StorageManager.kt** - 存储空间管理
5. **VoiceMonitorService.kt** - 录音完成回调

### 数据库位置
```
/data/data/com.voicelife.assistant/databases/voice_assistant.db
```

### 文件权限
- Android 6-12: 需要 WRITE_EXTERNAL_STORAGE 权限
- Android 13+: 写入Download目录不需要额外权限

## 总结

✅ **录音文件确实保存了**
- 物理文件: `/sdcard/Download/VoiceAssistant/pending/`
- 数据库记录: 应用私有数据库

✅ **双重存储的优势**
- 文件可以直接访问和播放
- 数据库支持快速查询和管理
- 两者相互补充，确保数据安全

✅ **当前状态**
- 8个录音文件，总计7.3MB
- 所有文件都在pending文件夹
- 可以随时播放和分享
