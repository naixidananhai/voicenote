# VoiceLife 24小时运行说明

## 核心设计

应用设计为**一次启动，持续运行**的模式：

1. **首次启动** - 点击"开始监听"按钮
2. **持续运行** - 服务在后台24小时运行
3. **无需重启** - 回到桌面、切换应用都不影响运行
4. **自动保活** - 使用前台服务保证不被系统杀死

## 正确使用方式

### 1. 首次启动

```
1. 打开应用
2. 授予所需权限（录音、通知、存储）
3. 点击"开始监听"按钮
4. 看到"✅ 服务正在后台运行，24小时监听中..."
5. 完成！可以最小化应用了
```

### 2. 日常使用

- **不要重复点击"开始监听"** - 服务已经在运行
- **可以随时查看** - 打开应用查看统计和日志
- **自动录音** - 检测到人声自动开始录音
- **自动保存** - 录音完成自动保存到数据库

### 3. 停止服务

只有在需要停止监听时才点击"停止监听"按钮

## 服务状态说明

### 运行中
- 按钮显示：红色"停止监听"
- 状态提示：✅ 服务正在后台运行，24小时监听中...
- 通知栏：显示前台服务通知

### 已停止
- 按钮显示：蓝色"开始监听"
- 状态提示：服务已停止
- 通知栏：无通知

## 代码优化说明

### 问题修复

**问题1：重复点击"开始监听"导致麦克风失效**
- 原因：每次点击都会停止并重启AudioRecord
- 修复：检测服务已运行时直接返回，不重启

**问题2：回到桌面后检测不到人声**
- 原因：AudioRecord可能处于异常状态
- 修复：添加健康检查和零数据检测

**问题3：服务状态不准确**
- 原因：UI状态与实际服务状态不同步
- 修复：每次onResume时检查真实服务状态

### 关键代码

#### VoiceMonitorService.kt
```kotlin
private fun startMonitoring() {
    // 如果录制器已经在运行，不要重启
    if (audioRecorder?.isRecording() == true) {
        debugLogger.i(TAG, "✅ 录制器已在运行，无需重启")
        return
    }
    
    // 只在需要时才初始化和启动
    // ...
}
```

#### MainViewModel.kt
```kotlin
fun loadData() {
    // 检查服务是否真的在运行
    val isServiceRunning = isServiceRunning()
    
    _uiState.value = _uiState.value.copy(
        isServiceRunning = isServiceRunning
    )
}
```

#### MainActivity.kt
```kotlin
override fun onResume() {
    super.onResume()
    // 每次回到前台时重新加载数据和检查服务状态
    viewModel.loadData()
}
```

## 保活策略

### 1. 前台服务
- 使用`startForeground()`
- 显示持久通知
- 系统优先级最高

### 2. START_STICKY
- 服务被杀后自动重启
- 保证长期运行

### 3. 电池优化白名单
- 建议用户添加到白名单
- 避免被系统休眠

### 4. 权限保持
- 运行时检查权限
- 权限丢失时显示警告

## 测试场景

### 场景1：正常启动
```
1. 打开应用
2. 点击"开始监听"
3. 对着手机说话
4. 查看日志：应该看到"🎤 检测到人声"
5. 查看文件：/sdcard/Download/VoiceAssistant/pending/
```

### 场景2：回到桌面
```
1. 服务运行中
2. 按Home键回到桌面
3. 等待几分钟
4. 重新打开应用
5. 检查：服务状态应该是"运行中"
6. 对着手机说话，应该能正常录音
```

### 场景3：切换应用
```
1. 服务运行中
2. 打开其他应用（如浏览器）
3. 使用其他应用几分钟
4. 返回VoiceLife
5. 检查：服务应该还在运行
```

### 场景4：锁屏
```
1. 服务运行中
2. 锁定屏幕
3. 等待几分钟
4. 解锁并打开应用
5. 检查：服务应该还在运行
```

### 场景5：重启应用
```
1. 服务运行中
2. 强制停止应用（设置 > 应用 > 强制停止）
3. 重新打开应用
4. 检查：服务状态应该是"已停止"
5. 需要重新点击"开始监听"
```

## 常见问题

### Q1: 为什么点击"开始监听"后没反应？
A: 如果服务已经在运行，按钮会保持红色"停止监听"状态，这是正常的。

### Q2: 为什么检测不到人声？
A: 检查以下几点：
- 麦克风权限是否授予
- 是否在真实设备上测试（模拟器麦克风不工作）
- 查看日志中的VAD概率值
- 确认音频数据不是全0

### Q3: 为什么服务会被杀掉？
A: 可能原因：
- 系统内存不足
- 电池优化限制
- 用户手动强制停止
- 解决：添加到电池优化白名单

### Q4: 如何确认服务正在运行？
A: 三种方式：
1. 应用内查看状态提示
2. 通知栏查看前台服务通知
3. 查看实时日志是否有新内容

### Q5: 录音文件保存在哪里？
A: `/sdcard/Download/VoiceAssistant/pending/`
- 可以在应用内点击"打开文件夹"查看
- 也可以用文件管理器直接访问

## 性能监控

### 关键指标

1. **VAD概率** - 应该在0.0-1.0之间波动
2. **音频数据** - 不应该全是0
3. **帧处理速度** - 每秒约31帧（16kHz/512）
4. **内存占用** - 待测试
5. **CPU占用** - 待测试
6. **电池消耗** - 待测试

### 日志关键字

- `🎤 检测到人声` - VAD检测成功
- `📝 开始录音` - 录音会话开始
- `✨ 录音完成` - 录音保存成功
- `⚠️ 警告：连续100帧音频数据为0` - 麦克风异常
- `✅ 录制器已在运行，无需重启` - 防止重复启动

## 下一步优化

1. **智能重启** - 检测到麦克风异常时自动重启
2. **网络同步** - 定期上传录音到云端
3. **电量优化** - 低电量时降低采样率
4. **存储优化** - 自动压缩和清理
5. **统计报告** - 每日/每周录音统计

## 总结

核心原则：**一次启动，持续运行，无需干预**

- ✅ 点击一次"开始监听"
- ✅ 服务在后台持续运行
- ✅ 自动检测人声并录音
- ✅ 自动保存到数据库
- ❌ 不要重复点击"开始监听"
- ❌ 不要频繁打开关闭应用
- ❌ 不要手动强制停止服务
