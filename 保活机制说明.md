# VoiceLife 保活机制说明

## 问题描述

当用户手动清理内存杀掉应用进程后，服务会停止，需要手动重新启动。这对于24小时持续监听的需求来说是不可接受的。

## 保活策略

### 1. 前台服务（最重要）✅
**优先级**: 最高
**实现**: 已完成

```kotlin
startForeground(notificationId, notification)
```

**效果**:
- 系统优先级最高
- 显示持久通知
- 不容易被系统杀死
- 用户可见，符合Android规范

### 2. START_STICKY ✅
**优先级**: 高
**实现**: 已完成

```kotlin
override fun onStartCommand(...): Int {
    return START_STICKY
}
```

**效果**:
- 服务被杀后系统会尝试重启
- 但intent可能为null
- 适合无状态服务

### 3. onTaskRemoved重启 ✅
**优先级**: 高
**实现**: 新增

```kotlin
override fun onTaskRemoved(rootIntent: Intent?) {
    // 检查服务是否应该运行
    if (shouldRun) {
        // 重启服务
        startForegroundService(restartIntent)
    }
}
```

**效果**:
- 应用被清理时触发
- 自动重启服务
- 保持运行状态

### 4. BroadcastReceiver监听 ✅
**优先级**: 中
**实现**: 新增

监听以下事件：
- `BOOT_COMPLETED` - 系统启动完成
- `MY_PACKAGE_REPLACED` - 应用更新完成
- `USER_PRESENT` - 用户解锁设备

```kotlin
class BootReceiver : BroadcastReceiver() {
    override fun onReceive(context: Context, intent: Intent) {
        when (intent.action) {
            Intent.ACTION_BOOT_COMPLETED -> startService()
            Intent.ACTION_MY_PACKAGE_REPLACED -> startService()
            Intent.ACTION_USER_PRESENT -> checkAndStartService()
        }
    }
}
```

**效果**:
- 系统重启后自动启动
- 应用更新后自动恢复
- 用户解锁后检查状态

### 5. 状态持久化 ✅
**优先级**: 中
**实现**: 新增

```kotlin
// 保存服务运行状态
SharedPreferences.edit()
    .putBoolean("service_was_running", true)
    .apply()

// 读取状态
val wasRunning = prefs.getBoolean("service_was_running", false)
```

**效果**:
- 记住服务是否应该运行
- 重启后恢复状态
- 避免不必要的启动

### 6. 电池优化白名单 ⏳
**优先级**: 中
**实现**: 待完善

```kotlin
// 请求加入白名单
val intent = Intent(Settings.ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS)
intent.data = Uri.parse("package:$packageName")
startActivity(intent)
```

**效果**:
- 避免被Doze模式限制
- 后台运行更稳定
- 需要用户手动授权

### 7. WorkManager定期检查 ⏳
**优先级**: 低
**实现**: 待实现

```kotlin
// 每15分钟检查一次服务状态
val workRequest = PeriodicWorkRequestBuilder<ServiceCheckWorker>(
    15, TimeUnit.MINUTES
).build()
```

**效果**:
- 定期检查服务是否运行
- 如果停止则重启
- 作为最后的保险

## 实现细节

### 文件结构
```
app/src/main/java/com/voicelife/assistant/
├── receiver/
│   └── BootReceiver.kt          # 系统事件接收器
├── service/
│   └── VoiceMonitorService.kt   # 前台服务
└── AndroidManifest.xml          # 权限和组件声明
```

### 权限声明
```xml
<!-- 自动启动权限 -->
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
<uses-permission android:name="android.permission.QUICKBOOT_POWERON" />
<uses-permission android:name="android.permission.REBOOT" />
```

### 接收器注册
```xml
<receiver
    android:name=".receiver.BootReceiver"
    android:enabled="true"
    android:exported="true"
    android:directBootAware="true">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.QUICKBOOT_POWERON" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
        <action android:name="android.intent.action.USER_PRESENT" />
    </intent-filter>
</receiver>
```

## 测试场景

### 场景1：手动清理内存
```
1. 启动服务
2. 打开系统任务管理器
3. 清理应用
4. 等待几秒
5. 检查：服务应该自动重启
```

### 场景2：系统重启
```
1. 启动服务
2. 重启手机
3. 系统启动完成后
4. 检查：服务应该自动启动
```

### 场景3：应用更新
```
1. 启动服务
2. 安装新版本APK
3. 更新完成后
4. 检查：服务应该自动恢复
```

### 场景4：用户解锁
```
1. 启动服务
2. 锁定屏幕
3. 等待一段时间
4. 解锁屏幕
5. 检查：如果服务停止应该重启
```

## 不同厂商的特殊处理

### 小米 (MIUI)
```
设置 > 应用设置 > 应用管理 > VoiceLife
- 自启动：开启
- 后台弹出界面：允许
- 显示悬浮窗：允许
- 锁屏显示：允许
```

### 华为 (EMUI)
```
设置 > 应用 > 应用启动管理 > VoiceLife
- 自动管理：关闭
- 手动管理：
  - 自动启动：开启
  - 后台活动：允许
  - 关联启动：允许
```

### OPPO (ColorOS)
```
设置 > 应用管理 > VoiceLife
- 自启动：开启
- 后台运行：允许
- 关联启动：允许
```

### vivo (Funtouch OS)
```
设置 > 更多设置 > 权限管理 > 自启动 > VoiceLife：开启
设置 > 电池 > 后台高耗电 > VoiceLife：允许
```

### 三星 (One UI)
```
设置 > 应用程序 > VoiceLife
- 电池 > 后台使用限制：无限制
- 自动启动：开启
```

## 日志关键字

### 正常运行
```
✅ 服务正在后台运行，24小时监听中...
✅ 录制器已在运行，无需重启
```

### 重启相关
```
⚠️ 应用被清理，尝试重启服务...
🔄 服务重启命令已发送
✅ 服务已自动重启
```

### 系统事件
```
📱 系统启动完成，检查服务状态
🔄 应用更新完成，恢复服务
🔓 用户解锁，检查服务状态
```

## 限制和注意事项

### Android系统限制
1. **Android 8.0+**: 后台服务限制更严格
2. **Android 9.0+**: 电池优化更激进
3. **Android 10+**: 后台位置访问限制
4. **Android 11+**: 后台启动限制
5. **Android 12+**: 前台服务通知要求更严格

### 厂商定制系统
- 小米、华为、OPPO、vivo等厂商有额外的后台限制
- 需要用户手动设置自启动权限
- 需要加入电池优化白名单

### 用户行为
- 用户可以强制停止应用
- 用户可以禁用自启动
- 用户可以撤销权限
- 用户可以卸载应用

### 最佳实践
1. **明确告知用户**: 说明需要后台运行
2. **引导设置**: 提供设置自启动的指引
3. **优雅降级**: 无法保活时提示用户
4. **节省资源**: 避免过度消耗电量和内存

## 效果预期

### 理想情况（所有机制生效）
- ✅ 99%的时间保持运行
- ✅ 被杀后5秒内自动重启
- ✅ 系统重启后自动启动
- ✅ 应用更新后自动恢复

### 实际情况（部分机制受限）
- ✅ 前台服务：90%有效
- ✅ START_STICKY：70%有效
- ✅ onTaskRemoved：60%有效
- ⚠️ BroadcastReceiver：50%有效（厂商限制）
- ⚠️ 电池优化：需要用户授权

### 最坏情况（所有机制失效）
- ❌ 用户强制停止
- ❌ 用户禁用自启动
- ❌ 系统极端省电模式
- 💡 解决：提示用户手动启动

## 监控和统计

### 添加统计
```kotlin
data class ServiceStats(
    val startCount: Int,        // 启动次数
    val restartCount: Int,      // 重启次数
    val killCount: Int,         // 被杀次数
    val totalUptime: Long,      // 总运行时间
    val lastRestartReason: String  // 最后重启原因
)
```

### 显示在UI
```
服务统计:
- 运行时间: 23小时45分钟
- 启动次数: 1次
- 重启次数: 2次
- 最后重启: 系统清理内存
- 稳定性: 99.8%
```

## 总结

### 已实现的保活机制
✅ 前台服务（最重要）
✅ START_STICKY
✅ onTaskRemoved重启
✅ BroadcastReceiver监听
✅ 状态持久化

### 待实现的增强
⏳ 电池优化白名单引导
⏳ WorkManager定期检查
⏳ 厂商特殊处理
⏳ 统计和监控

### 关键建议
1. **前台服务是核心** - 必须保持
2. **多层防护** - 不依赖单一机制
3. **用户引导** - 说明需要的权限
4. **优雅降级** - 无法保活时提示
5. **真实设备测试** - 不同厂商表现不同

### 预期效果
在大多数情况下，服务可以保持运行。即使被杀，也能在几秒内自动重启。但需要用户配合设置自启动权限。
