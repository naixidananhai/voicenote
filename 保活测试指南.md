# VoiceLife 保活测试指南

## 已实现的保活机制

✅ **前台服务** - 最高优先级，显示持久通知
✅ **START_STICKY** - 服务被杀后系统自动重启
✅ **onTaskRemoved** - 应用被清理时自动重启
✅ **BroadcastReceiver** - 监听系统事件自动启动
✅ **状态持久化** - 记住服务运行状态

## 测试步骤

### 测试1：手动清理内存（你遇到的问题）

**步骤**：
1. 在手机上打开VoiceLife
2. 点击"开始监听"
3. 观察通知栏出现前台服务通知
4. 打开系统任务管理器（多任务界面）
5. 向上滑动清理VoiceLife应用
6. **等待5-10秒**
7. 重新打开VoiceLife

**预期结果**：
- ✅ 按钮显示"停止监听"（红色）
- ✅ 状态显示"服务正在后台运行"
- ✅ 通知栏有前台服务通知
- ✅ 日志中有"🔄 服务重启命令已发送"

**如果失败**：
- 检查日志中是否有"⚠️ 应用被清理"
- 检查是否有权限问题
- 尝试在设置中开启自启动权限

### 测试2：系统重启

**步骤**：
1. 启动服务
2. 重启手机
3. 系统启动完成后
4. 打开VoiceLife

**预期结果**：
- ✅ 服务自动启动
- ✅ 按钮显示"停止监听"
- ✅ 通知栏有前台服务通知

### 测试3：应用更新

**步骤**：
1. 启动服务
2. 安装新版本APK（覆盖安装）
3. 更新完成后
4. 打开VoiceLife

**预期结果**：
- ✅ 服务自动恢复
- ✅ 状态正确显示

### 测试4：长时间运行

**步骤**：
1. 启动服务
2. 最小化应用
3. 正常使用手机24小时
4. 定期检查服务状态

**预期结果**：
- ✅ 服务持续运行
- ✅ 能正常录音
- ✅ 电池消耗合理

## 查看日志

### 方法1：应用内日志
打开VoiceLife，查看底部的实时日志面板

### 方法2：ADB日志
```bash
adb logcat -s VoiceAssistant:* VoiceMonitorService:* BootReceiver:*
```

### 关键日志

**服务启动**：
```
✅ 服务正在后台运行，24小时监听中...
✅ 音频录制器已启动，等待人声...
```

**应用被清理**：
```
⚠️ 应用被清理，尝试重启服务...
🔄 服务重启命令已发送
```

**系统事件**：
```
📱 系统启动完成，检查服务状态
🔄 应用更新完成，恢复服务
```

**自动重启成功**：
```
✅ 服务已自动重启
✅ 录制器已在运行，无需重启
```

## 厂商特殊设置

### 小米手机
```
设置 > 应用设置 > 应用管理 > VoiceLife
- 自启动：✅ 开启
- 后台弹出界面：✅ 允许
- 显示悬浮窗：✅ 允许
- 锁屏显示：✅ 允许

设置 > 省电与电池 > 应用智能省电 > VoiceLife
- 选择：无限制
```

### 华为手机
```
设置 > 应用 > 应用启动管理 > VoiceLife
- 自动管理：❌ 关闭
- 手动管理：
  - 自动启动：✅ 开启
  - 后台活动：✅ 允许
  - 关联启动：✅ 允许

设置 > 电池 > 应用启动管理 > VoiceLife
- 选择：手动管理
```

### OPPO手机
```
设置 > 应用管理 > VoiceLife
- 自启动：✅ 开启
- 后台运行：✅ 允许
- 关联启动：✅ 允许

设置 > 电池 > 高耗电应用 > VoiceLife
- 选择：允许后台运行
```

### vivo手机
```
设置 > 更多设置 > 权限管理 > 自启动 > VoiceLife
- ✅ 开启

设置 > 电池 > 后台高耗电 > VoiceLife
- ✅ 允许
```

### 三星手机
```
设置 > 应用程序 > VoiceLife
- 电池 > 后台使用限制：无限制
- 自动启动：✅ 开启

设置 > 设备维护 > 电池 > 应用程序电源管理
- VoiceLife：不优化
```

## 常见问题

### Q1: 清理内存后服务没有自动重启？

**可能原因**：
1. 厂商限制了自启动
2. 电池优化限制
3. 权限不足

**解决方案**：
1. 检查自启动权限（见上面的厂商设置）
2. 加入电池优化白名单
3. 手动重新启动服务

### Q2: 系统重启后服务没有自动启动？

**可能原因**：
1. BOOT_COMPLETED权限被禁用
2. 厂商限制了开机自启动

**解决方案**：
1. 检查AndroidManifest.xml中的权限
2. 在系统设置中开启自启动
3. 重启后手动启动一次

### Q3: 服务频繁重启？

**可能原因**：
1. 内存不足
2. 系统激进的电池优化
3. 代码异常

**解决方案**：
1. 查看日志找出原因
2. 优化内存使用
3. 加入电池优化白名单

### Q4: 通知栏没有显示前台服务？

**可能原因**：
1. 通知权限未授予
2. 通知被用户关闭
3. 服务未正确启动

**解决方案**：
1. 检查通知权限
2. 在系统设置中开启通知
3. 重新启动服务

## 调试技巧

### 1. 检查服务是否运行
```bash
adb shell dumpsys activity services com.voicelife.assistant
```

### 2. 检查进程
```bash
adb shell ps | grep voicelife
```

### 3. 检查SharedPreferences
```bash
adb shell run-as com.voicelife.assistant cat shared_prefs/voice_assistant_prefs.xml
```

### 4. 强制停止测试
```bash
adb shell am force-stop com.voicelife.assistant
# 等待几秒
adb shell dumpsys activity services com.voicelife.assistant
```

### 5. 模拟系统事件
```bash
# 模拟开机完成
adb shell am broadcast -a android.intent.action.BOOT_COMPLETED

# 模拟用户解锁
adb shell am broadcast -a android.intent.action.USER_PRESENT
```

## 性能监控

### 查看电池消耗
```bash
adb shell dumpsys batterystats | grep voicelife
```

### 查看内存占用
```bash
adb shell dumpsys meminfo com.voicelife.assistant
```

### 查看CPU占用
```bash
adb shell top | grep voicelife
```

## 预期效果

### 理想情况
- ✅ 清理内存后5秒内自动重启
- ✅ 系统重启后自动启动
- ✅ 24小时持续运行
- ✅ 电池消耗 < 5%/天

### 实际情况（取决于厂商）
- ✅ 小米/华为/OPPO/vivo：需要手动设置自启动
- ✅ 三星/原生Android：通常工作良好
- ⚠️ 部分厂商：可能需要多次设置

### 最坏情况
- ❌ 用户强制停止：无法自动重启
- ❌ 用户禁用自启动：无法自动启动
- 💡 解决：提示用户手动启动

## 下一步优化

### 短期
- [ ] 添加电池优化白名单引导
- [ ] 添加自启动权限引导
- [ ] 添加服务状态监控

### 中期
- [ ] WorkManager定期检查
- [ ] 厂商特殊处理
- [ ] 统计和分析

### 长期
- [ ] 云端监控
- [ ] 智能省电
- [ ] 机器学习优化

## 总结

现在应用已经具备了较强的保活能力：

✅ **多层防护** - 5种保活机制
✅ **自动重启** - 被杀后自动恢复
✅ **状态持久化** - 记住运行状态
✅ **系统事件监听** - 开机/更新自动启动

**关键**：需要用户配合设置自启动权限，特别是在小米、华为、OPPO、vivo等厂商手机上。

**测试建议**：
1. 先在真实设备上测试基本功能
2. 然后测试清理内存场景
3. 最后测试长时间运行
4. 根据日志调整优化
